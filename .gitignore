# Generated artifacts — recompute from scripts, not committed by default.
output/exports/*
!output/exports/.gitkeep

# 07_server.py disk-backed request cache — runtime-generated, regenerate on demand.
cache/

# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/
env/
.env
*.egg-info/

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/
*.swp
*~

# Local server / dev logs
*.log
nohup.out

# Raw OSM dumps from one-off experiments
*_raw.json
*_subs.json
overpass_*.json
hartford_towns_raw.json

# NCEI Storm Events bulk downloads (15_fetch_storm_events.py) -- multi-MB
# nationwide gzips, re-downloadable, not meant to be committed.
data/_ncei_cache/

# User's scratch / experimental simulation copies
hartford_county_simulation*.html
!03_grid_simulation.html

# Word/Office lock files
~$*

# Local-only development journals (kept on disk for Google Docs conversion)
DEVELOPMENT_JOURNAL*.md

# Rust build output (regenerate with cargo build)
wasm_scheduler/target/
wasm_scheduler/Cargo.lock

# --- Research / analysis artifacts: kept locally, NOT part of the runnable app ---
# The deployable simulator is app + server + schedulers + the data the app loads.
# Everything below is research, docs, or generated output -- present on disk, but
# intentionally untracked so the repo stays to what builds/runs the code.
# Analysis & validation scripts:
/16_calibrate_against_real_storms.py
/19_validate_against_eaglei.py
/20_ensemble_vs_actual.py
/21_crew_backout.py
/22_infer_network_from_outages.py
/23_concentrated_placement.py
/24_validate_placement_vs_eaglei.py
/25_placement_fidelity_summary.py
/26_storm_applicability_census.py
# Research/validation data (the browser app does not load these):
/data/eaglei_ct_*.csv
/data/connecticut_storm_events_*.json
# Generated figures & exports (regenerate via the scripts above):
/output/*.png
/output/*.csv
# Findings, journals, notebooks, office docs, reference text:
/PLACEMENT_FIDELITY_FINDING.md
/ROADMAP.md
/STORM_TIMELINE_IMPLEMENTATION_PLAN.md
/DESIGN_DECISIONS.md
/JOURNAL.html
/Hartford_Grid_Dev_Journal.docx
/Hartford_Grid_Research_Context.docx
/build_docx.py
/_wanik_2015.txt
/algorithm_methodology.ipynb
/fetch_hrrr_storm_wind.ipynb
# Professor-facing brief (never auto-publish):
/research_brief_public_data_outages.pdf
/make_research_brief.py
